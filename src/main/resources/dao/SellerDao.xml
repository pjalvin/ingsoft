<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.SellerDao">
    <insert id="createSeller" parameterType="bo.ucb.edu.ingsoft.model.Seller">
        INSERT into seller VALUES
        (null,#{idPerson},#{idUser},1,#{imagePath},#{transaction.txDate},
        #{transaction.txIduser},#{transaction.txHost},#{transaction.txUpdate})
    </insert>
    <select id="findBySellerId" resultType="bo.ucb.edu.ingsoft.model.Seller">
        SELECT
               id_seller as idSeller,
               id_person as idPerson,
               id_user as idUser,
               status as status,
               image_path as imagePath
        FROM seller
        WHERE
            id_seller = #{idSeller}
    </select>
    <update id="updateSeller" parameterType="bo.ucb.edu.ingsoft.model.Seller">
        UPDATE seller SET
        id_seller=#{idSeller},id_person=#{idPerson},id_user=#{idUser},status=1,image_path=#{imagePath}, tx_date=#{transaction.txDate},
        tx_id_user=#{transaction.txIduser},tx_host=#{transaction.txHost},tx_update=#{transaction.txUpdate}
        WHERE id_seller=#{idSeller}
    </update>
    <select id="publications" resultType="bo.ucb.edu.ingsoft.dto.PublicationSimpleRequest">
        SELECT
               id_publication as idSeller,
               image_path as imagePath,
               titulo as titulo,
               price as price
        FROM publication
        INNER JOIN seller on
        publication.id_seller=seller.id_seller
        INNER JOIN user on
        seller.id_user=user.id_user
        INNER JOIN image_publication
        image_publication.id_publication=publication.id_publication
        WHERE
            user.id_user = #{idUsuario}
        LIMIT 1;
    </select>
</mapper>